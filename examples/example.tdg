schema ExampleSchema {
    description: "Basic test schema"
    seed: 42
    strategy: random
    combination_strategy: full

    entity User {
        fields {
            name: string
            age: int
        }
    }
}